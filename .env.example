# Copy this file to `.env` and fill in your values, then restart `npm run dev`.

# Google Analytics 4 Measurement ID (looks like G-XXXXXXXXXX).
# Leave empty to disable analytics entirely (no GA script, no cookies).
VITE_GA_ID=
